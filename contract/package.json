{
  "name": "contract",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "hardhat compile",
    "test": "hardhat test",
    "deploy": "hardhat run scripts/deploy.ts --network mantle",
    "deploy:testnet": "hardhat run scripts/deploy.ts --network mantleTestnet",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@nomicfoundation/hardhat-ethers": "^3.0.0",
    "hardhat": "^2.22.0",
    "ethers": "^6.13.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.7.0",
    "@types/node": "^20"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0"
  }
}
