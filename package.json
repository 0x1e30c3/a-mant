{
  "name": "a-mant",
  "private": true,
  "version": "0.1.0",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "dev:agent": "pnpm --filter agent dev",
    "build:web": "pnpm --filter web build",
    "build:contract": "pnpm --filter contract build",
    "test:contract": "pnpm --filter contract test",
    "deploy:contract": "pnpm --filter contract deploy"
  },
  "workspaces": [
    "web",
    "contract",
    "agent"
  ]
}
